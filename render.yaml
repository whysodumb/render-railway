services:
  # A Docker web service
  - type: web
    name: anything
    env: docker
    region: Frankfurt
    plan: free
    envVars:
      - key: PORT
        value: 80
      - key: BOT_CONFIG_URL
        # value: BOT_CONFIG_URL
        sync: false
